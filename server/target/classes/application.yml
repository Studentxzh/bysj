server:
  port: 9000

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campus_textbook_recycle?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=UTF-8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    root: INFO
    com.ncu.textbook: DEBUG

